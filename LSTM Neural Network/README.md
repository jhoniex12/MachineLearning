# Introduction

In this project, we will be predicting stock prices using a Long Short-Term Memory (LSTM) neural network. Stock price data from three Australian companies BHP, CBA, and WES are used. Since stock prices are time series data, LSTM is suitable because it can remember past information in sequences. Several experiments are done by changing the number of neurons and training epochs. The results are compared using prediction graphs and RMSE values.
